/*
use crate::Sector;
use std::collections::HashMap;

pub const MAX_PRODUCTION: u32 = 150;
pub const MIN_PRODUCTION: u32 = 10;

pub struct Planet {
    pub key: u32,
    pub pos: Sector,
    pub name: String,
    pub owner: u32,
    pub year_owned: u32,
    pub prev_owner: u32,
    pub max_production: u32,
    pub current_production: u32,
    pub production_points: u32,
    pub armies: u32,
    pub ground_batteries: u32,
    pub stardock: Vec<u32>,
}

// Player planet Database
pub struct PlanetDB {
    // HashMap keys = planet's key
    pub name: HashMap<u32, String>,
    pub year_scouted: HashMap<u32, u32>,
    pub year_viewed: HashMap<u32, u32>,
    pub year_owned: HashMap<u32, u32>,
    pub max_prod: HashMap<u32, u32>,
    pub cur_prod: HashMap<u32, u32>,
    pub production_points: HashMap<u32, u32>,
    pub owner: HashMap<u32, u32>,
    pub prev_owner: HashMap<u32, u32>,
    pub owned_for: HashMap<u32, u32>,
    pub star_dock: HashMap<u32, u32>,
    pub armies: HashMap<u32, u32>,
    pub ground_batteries: HashMap<u32, u32>,
    pub pos: HashMap<u32, u32>,
}
*/
