// EC4X Construction Configuration
// Construction times, costs, repair, and upkeep for all facilities and ships
//
// This file contains:
// - BALANCE VARIABLES: Construction times, costs, repair costs, upkeep
// - CORE MECHANICS: Construction rules and requirements

// ============================================================================
// CONSTRUCTION TIMES AND RULES
// ============================================================================

construction {
    // BALANCE: Construction times (in turns/months)
    spaceportTurns 1
    spaceportDocks 5                // Number of construction docks

    shipyardTurns 2                 // Military ship construction time (turns)
    shipyardDocks 10                // Number of construction docks
    shipyardRequiresSpaceport #true // Must have spaceport to build shipyard

    starbaseTurns 3
    starbaseRequiresShipyard #true  // Must have shipyard to build starbase
    starbaseMaxPerColony 3        // Maximum 3 starbases per colony

    planetaryShieldTurns 1         // Can rebuild in one turn
    planetaryShieldMax 1           // Only one shield per planet
    planetaryShieldReplaceOnUpgrade #true  // Must salvage old to upgrade

    groundBatteryTurns 1           // Single turn construction
    groundBatteryMax -1            // No limit (-1 = unlimited)

    // MECHANIC: Game design rules
    fighterSquadronPlanetBased #true  // Fighters are planet-based, not fleet
}

// ============================================================================
// REPAIR AND MODIFIERS
// ============================================================================

repair {
    // BALANCE: Repair costs and times
    shipRepairTurns 1              // Time to repair crippled ship at shipyard
    shipRepairCostMultiplier 0.25 // Ship repair cost = 25% of unit PC
    starbaseRepairCostMultiplier 0.25  // Starbase repair cost = 25% of PC
}

modifiers {
    // BALANCE: Construction cost modifiers
    planetsideConstructionCostMultiplier 2.0  // 100% PC increase for planet-side ship construction
    constructionCapacityIncreasePerLevel 0.10  // CST increases capacity by 10% per level
}

// ============================================================================
// BUILD COSTS
// ============================================================================

costs {
    // BALANCE: Build costs for infrastructure (not ships - those in ships.kdl)
    spaceportCost 500
    shipyardCost 1000
    starbaseCost 1200
    groundBatteryCost 50  // Same as Battleship - defensive hardpoint with heavy weapons
    fighterSquadronCost 20         // Cost per fighter squadron

    // Planetary shield costs by tech level
    planetaryShieldSld1Cost 500
    planetaryShieldSld2Cost 750
    planetaryShieldSld3Cost 1000
    planetaryShieldSld4Cost 1500
    planetaryShieldSld5Cost 2000
    planetaryShieldSld6Cost 3000
}

// ============================================================================
// UPKEEP COSTS
// ============================================================================

upkeep {
    // BALANCE: Annual upkeep costs
    spaceportUpkeep 25
    shipyardUpkeep 50
    starbaseUpkeep 75
    groundBatteryUpkeep 5
    planetaryShieldUpkeep 50
}
