// EC4X Starmap Configuration v0.2
// Source: docs/specs/02-assets.md Section 2.1
// Defines parameters for procedural map generation.

// --- Jump Lane Generation ---
lanes {
    (docs)"docs/specs/02-assets.md#section-2-1"
    // Defines the distribution and behavior of jump lane types.
    // Major and Minor lanes allow ALL ship types.
    // Restricted lanes block crippled ships only.
    distribution {
        major 0.50
        minor 0.35
        restricted 0.15
    }
}

// --- Homeworld Placement ---
homeworld {
    (docs)"docs/specs/02-assets.md#section-2-1"
    // Homeworlds are placed using distance maximization to ensure fair spacing.
    guaranteedLaneCount 3      // Each homeworld is guaranteed this many lanes.
    guaranteedLaneType "Major" // These must be Major lanes.
}

// --- Hub System ---
hub {
    (docs)"docs/specs/02-assets.md#section-2-1"
    guaranteedLaneCount 6  // The central hub system is guaranteed to have 6 lanes.
    mixedLanes true        // Hub uses the standard lane distribution.
}
